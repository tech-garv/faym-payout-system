## Environment Variables

Create a `.env` file by copying `.env.example` and updating it with your local MySQL configuration.
PORT=5000

DB_HOST=localhost
DB_USER=your_mysql_username
DB_PASSWORD=your_mysql_password
DB_NAME=faym_payout
